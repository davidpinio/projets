{% extends 'base.html.twig' %}

{% block title %}Articles{% endblock %}

{% block body %}
    <h1>LES ARTICLES</h1>

    <div class="container">
        <h1>{{ article.title }}</h1>
        <p>
            {{ article.description }}
        </p>
     
        <figure>
            <img src="https://picsum.photos/200/300" alt="photo">
        </figure>
        <small class="text-muted">Date de publication : {{ article.createdAt|date("d/m/Y H:i:s") }}</small>
    </div>

    {# <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ article.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ article.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ article.description }}</td>
            </tr>
            <tr>
                <th>Published</th>
                <td>{{ article.published ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ article.image }}</td>
            </tr>
            <tr>
                <th>Create_At</th>
                <td>{{ article.createAt ? article.createAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table> #}

    <a href="{{ path('article_index') }}"><i class="fa fa-reply"></i>Retour</a>

    <a href="{{ path('article_edit', {'id': article.id}) }}"><i class="fa fa-edit"></i>Ajouter</a>

    {{ include('article/_delete_form.html.twig') }}
{% endblock %}
